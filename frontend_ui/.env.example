# Base URL del core_management_api (FastAPI). Si no se define,
# controlService.ts usa http://localhost:8001 como fallback.
# Otros servicios todavía usan URLs hardcodeadas (T-XXX post-TB1: migrar todo).
VITE_API_BASE_URL=http://localhost:8001
